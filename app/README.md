# 🩺 Diabetes Prediction App (Streamlit + SVM)

A simple ML project using Support Vector Machine to predict diabetes based on medical features.

## Run locally
```bash
pip install -r requirements.txt
streamlit run app.py
